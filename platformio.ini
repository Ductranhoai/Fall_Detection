; ; PlatformIO Project Configuration File
; ;
; ;   Build options: build flags, source filter
; ;   Upload options: custom upload port, speed and extra flags
; ;   Library options: dependencies, extra library storages
; ;   Advanced options: extra scripting
; ;
; ; Please visit documentation for the other options and examples
; ; https://docs.platformio.org/page/projectconf.html

; [env:esp32doit-devkit-v1]
; platform = espressif32
; board = esp32doit-devkit-v1
; framework = espidf
; monitor_speed = 115200
; board_build.f_cpu = 240000000L
; board_upload.flash_size = 2MB

; ; Partition table (nếu cần OTA)
; ; board_build.partitions = partitions.csv

; ; Libraries từ PlatformIO registry
; lib_deps = 
;     ; ; MPU6050
;     ; adafruit/Adafruit MPU6050@^2.2.4
;     ; adafruit/Adafruit Unified Sensor@^1.1.6
    
;     ; ; GPS
;     ; mikalhart/TinyGPSPlus@^1.0.3
    
;     ; ; JSON
;     ; bblanchon/ArduinoJson@^6.21.3
    
;     ; ; BLE
;     ; ; ESP32 BLE Arduino đã có sẵn trong framework
    
;     ; ; SIM800L
;     ; ; Sử dụng thư viện tự viết hoặc TinyGSM

; build_flags = 
;     ; Enable features (có thể bật/tắt từng module)
;     -D CONFIG_ENABLE_WIFI=1
;     -D CONFIG_ENABLE_BLE=1
;     -D CONFIG_ENABLE_SIM=1
;     -D CONFIG_ENABLE_GPS=1
;     -D CONFIG_ENABLE_MPU6050=1
;     -D CONFIG_ENABLE_FALL_DETECTION=1
    
;     ; Debug level
;     -D LOG_LOCAL_LEVEL=4
;     -DCORE_DEBUG_LEVEL=5

; ; Cấu hình serial cho debug
; ; [env:esp32doit-devkit-v1-debug]
; ; debug_build_flags = -O0 -ggdb
; ; debug_tool = esp-prog

[env:esp32doit-devkit-v1]
platform = espressif32
board = esp32doit-devkit-v1
framework = espidf

monitor_speed = 115200
upload_protocol = esptool